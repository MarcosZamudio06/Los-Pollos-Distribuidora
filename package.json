{
  "name": "pollos-distribuidor",
  "version": "1.0.0",
  "description": "TypeScript monorepo foundation for the chicken distributor system.",
  "private": true,
  "scripts": {
    "frontend:test:e2e:browser": "npm --prefix frontend run test:e2e:browser --",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:preview": "npm --prefix frontend run preview",
    "backend:build": "npm --prefix backend run build",
    "backend:format": "npm --prefix backend run format",
    "backend:start": "npm --prefix backend run start",
    "backend:start:dev": "npm --prefix backend run start:dev",
    "backend:lint": "npm --prefix backend run lint",
    "backend:test": "npm --prefix backend test",
    "backend:test:e2e": "npm --prefix backend run test:e2e",
    "audit:dependencies": "audit-ci --directory . --config audit-ci.jsonc && audit-ci --directory backend --config audit-ci.jsonc && audit-ci --directory frontend --config audit-ci.jsonc",
    "test:guard": "node scripts/forbid-exclusive-tests.mjs",
    "test": "npm run backend:test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "headroom-ai": "^0.22.4",
    "lucide-react": "^1.23.0",
    "motion": "^12.42.2",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "audit-ci": "7.1.0",
    "shadcn": "^4.13.0"
  }
}
