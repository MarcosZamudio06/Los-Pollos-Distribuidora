NODE_ENV=production


FRONTEND_PORT=3000
BACKEND_PORT=4000
POSTGRES_PORT=5433

PORT=4000
API_PREFIX=api
SWAGGER_PATH=docs
CORS_ORIGIN=http://localhost:3000

DATABASE_SSL=false
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pollo_distribucion
# Keep POSTGRES_* above DATABASE_URL so Docker Compose can interpolate them when this file is copied to .env.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
SEED_ADMIN_PASSWORD=@dminPollos!
JWT_ACCESS_SECRET=local_access_change_me
JWT_REFRESH_SECRET=local_refresh_change_me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

VITE_API_URL=/api
